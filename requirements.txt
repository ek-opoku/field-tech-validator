streamlit
pandas
altair
pydeck
ortools
folium>=0.15.0
streamlit-folium>=0.17.0
plotly>=5.18.0
streamlit-geolocation>=0.0.10

# PDF reporting
reportlab

# Optional (edge OCR / voice SOP). Keep here so `pip install -r requirements.txt`
# brings a fully featured demo environment; the edge app handles absence gracefully.
pillow
pytesseract
vosk

pypdf
python-docx
